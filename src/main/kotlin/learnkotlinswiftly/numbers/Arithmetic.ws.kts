package learnkotlinswiftly.numbers

import kotlin.math.pow

val calculation = 4 * 2 + (4 - 5) / 19 //Regular math; to multiply, use *
println(calculation)

val remainder = 13F % 7F //To find the remainder of a quotient
println(remainder)

val fiveRaisedTo10 = 5.0.pow(10) //To raise to power (number to be raised must be Float/Double)
println(fiveRaisedTo10)