package learnkotlinswiftly.variables

var myString: String? = null //To mark as nullable, use explicit typing and append ? to variable type
println(myString)

myString = "Dog"
println(myString)

myString = null //To assign null to a variable
println(myString)