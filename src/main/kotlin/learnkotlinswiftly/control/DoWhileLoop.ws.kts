package learnkotlinswiftly.control

//Do-while loop performs the do { } FIRST, then continues if the condition in the while () is true,
var x = -1

do {
    println(x)
    x++
} while (x < 5)
